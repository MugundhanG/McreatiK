# ============================================
# Production overrides for `vite build` (npm run deploy)
# Vite automatically prefers this file over .env when building
# in production mode, so .env can stay pointed at localhost for
# local dev without needing to be toggled before every deploy.
# ============================================

VITE_API_BASE_URL=https://api.mcreatik.com
